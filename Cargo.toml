[package]
name = "kos-rs"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
kos-sys = { path = "/opt/toolchains/dc/rust/kos-sys" }

[patch.crates-io]
libc = { path = "/opt/toolchains/dc/rust/libc" }
